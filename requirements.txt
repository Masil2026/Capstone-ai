# --- Web Framework & Server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Environmental Variables ---
python-dotenv==1.0.1
pydantic-settings==2.5.2

# --- AI & LLM ---
pydantic-ai-slim[google]==1.93.0
google-genai

# --- Database & Cache ---
sqlalchemy==2.0.32
greenlet==3.5.2  # SQLAlchemy async(create_async_engine) 필수 — 일부 환경에선 자동설치 안 됨
asyncpg==0.30.0
# psycopg[binary,pool]==3.2.3  # 동기 드라이버 — asyncpg 전환 후 미사용
# psycopg2-binary               # 동기 드라이버 — asyncpg 전환 후 미사용
redis==5.0.8

# --- Testing ---
pytest==8.3.4
pytest-asyncio==1.4.0
deepeval==3.4.6  # AI 성능 평가 (골든셋 기반, tests/eval)

# --- Auth & JWT (Clerk 토큰 검증용) ---
python-jose[cryptography]==3.3.0

# --- HTTP Client ---
httpx==0.28.1
